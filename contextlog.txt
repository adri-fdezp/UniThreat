[2026-01-31] Project Finalization & Advanced OSINT Logic

### Backend & OSINT Engine Improvements
- **Modular Search Engine System**:
    - Project restructured into a proper Python package (`search_engines` and `risk_profiling`).
    - Implemented `GoogleSearch` class utilizing **Selenium WebDriver** for robust scraping and manual CAPTCHA solving.
- **Advanced Dorking Strategy**:
    - The profiler now executes specialized Google Dorks: Social Media (FB, IG, TW, TikTok), Professional (LinkedIn, GitHub), and Document Discovery (PDF, CVs).
- **Relevance Scoring Engine**:
    - Created a scoring algorithm that prioritizes results where the full target name appears in the title or description (+10/5 pts).
    - Ensures precise hits (exact name matches) appear at the top of the report.
- **Rich Data Extraction**:
    - Added logic to capture thumbnail images (profile pictures) from Google previews.

### Frontend & UI/UX Updates
- **Global Data Dashboard**: 
    - Eliminated the selection step; all findings are now combined, sorted by relevance, and displayed in a high-density "Analysis Report" grid.
- **Rich Media Support**:
    - Implemented side-by-side card layouts to display profile pictures next to metadata and snippets.
- **Modular Architecture**: 
    - UI broken down into `Header`, `SearchForm`, `ProfileResults`, and `DetailInspector` for better maintainability.
- **Compact Aesthetics**: 
    - Tightened padding and typography for a professional, "IDE-style" terminal workspace.

### System Utilities
- **Deployment**: `start_app.bat` handles simultaneous launch of Backend (port 5000) and Frontend (port 5173).
- **Current Status**: Fully functional prototype for masters thesis reconnaissance module.
